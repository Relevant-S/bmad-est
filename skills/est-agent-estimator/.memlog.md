---
updated: 2026-09-07T21:32
---

- (decision) Agent type is STATELESS. The module already owns durable state at _bmad/memory/est/ (cost model, ledger, comparables, calibration log) written under audit by the three workflows. A sanctum would be a second, competing memory surface for the same facts, and the one that drifts is the one nobody audits.
- (decision) Named Nadia, Delivery Estimator. User chose the name; user_name is already 'Estimator' so a role-only name would have collided in every greeting.
- (decision) Model curation is allowed on judgement alone, but only through a script with apply.py's guarantees (named human, mandatory why, backup, TTY-only, calibration-log entry) and tagged source=judgement. User chose this over observations-only. Reasoning: day one has zero actuals, and refusing the edit sends people to hand-edit cost-model.json, which has no backup, no log and no provenance.
- (decision) curate.py lives in est-calibrate/scripts, not in the agent, so one directory owns every write to the cost model and shares the backup/rollback/log code. The agent routes to it and carries no model-writing code of its own.
- (decision) Dropped a planned risk-read.py. Bid/no-bid signals (danger-quadrant share, band width, outside-scope share) are already fields in estimate.json; a script would only re-read them. The judgement is prompt work.
- (decision) inventory_from() moved from est-calibrate/backtest.py into est-estimate/estimate.py, beside the pricing it inverts. Both backtest and scenario now call one definition rather than two that could disagree about a field neither owner noticed. Build Standard 7.
- (event) scenario.py built: what-ifs, retags, profile overrides, change-request pricing and budget cut-lines, all through est-estimate's engine. 36 tests.
- (event) portfolio.py built: one pass over every estimate workspace and ledger entry, with staleness, next_action and attention signals. 20 tests.
- (note) Stale-bytecode trap found while mutation-testing: this machine's python3.9 caches pyc centrally at ~/Library/Caches/com.apple.python, and a same-length source edit within one second defeats its mtime+size invalidation. Two tests appeared to fail against correct code. Run tests with -B.
- (event) Analyze dispatched: six base lenses (leanness, architecture, determinism, customization, enhancement, cohesion). Sanctum lens skipped — the builder's prepass.py misclassifies this agent as 'memory' because it greps for _bmad/memory/, which here is the module's shared memory owned by three workflows, not a per-agent sanctum.
- (decision) Skipped the shipped prompt-quality-canon.md copy the agent build process emits unconditionally. Its stated reason is so an EVOLVING agent resolves the standard from its own root; this agent is stateless and authors no capabilities, so the copy would be an orphaned file — the exact defect the architecture lens caught twice on skills #2 and #4.
- (decision) No customize.toml override surface. Metadata block only. The module already has three configuration surfaces reaching this agent (config.yaml est section, company-profile.md, cost-model.json); a fourth is where they start to disagree, and house rules have a home in company-profile.md where est-estimate reads them too.
- (event) Cut-line search rebuilt three times: added a prune pass (it was proposing cuts the budget did not need), changed tier ordering from biggest-saving to smallest-sufficient (it proposed cutting payments where three peripheral features would do), and switched candidate scoring from baseline savings to marginal re-prices (baseline figures overstate a candidate whose closure is already partly dropped). All three passed the totals-based tests; the invariant tests caught them.
- (event) Module-wide defect found by a dangling-path sweep, not by a lens: all four skills read {project-root}/_bmad/config.yaml, but this installation stores config as TOML (_bmad/config.toml). Every skill would have silently fallen back to defaults, losing user_name, output_folder and all ten est_* settings. All four now resolve through _bmad/scripts/resolve_config.py, verified against the real config (core.user_name resolves to 'Estimator').
- (event) analyze: grade good, 0 critical / 9 high / 18 medium / 11 low, report .analysis/2026-09-07-2109/agent-analysis-report.html. All 38 addressed (2 partial: determinism-4 and -5, whose prose halves were fixed and whose script halves remain optional).
- (note) Three of the nine high findings were the agent contradicting a rule it states about itself: SKILL.md forbade inline scope extraction one row above a reference that did it; the non-negotiable promised decomposition on demand while scenario.py returned a headline that stopped at the phase split; triage.md claimed 'no separate calculation, only a read' then named three ratios nothing exposed. A stated rule is not a guarantee until something enforces it.
